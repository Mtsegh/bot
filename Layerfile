FROM node:16

COPY / /root
WORKDIR /root

RUN npm install

# Use pm2 to manage your bot in the background
RUN npm install -g pm2

# Start the bot using pm2 in the background
RUN BACKGROUND pm2 start npm -- start

EXPOSE WEBSITE localhost:8000
